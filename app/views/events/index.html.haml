%h1 Listing events

%table.table.table-hover.table-condensed
  %tr
    %th Title
    %th Date
    %th User

  - @events.each do |event|
    %tr
      %td= link_to event.title, event_path(event)
      %td= distance_of_time_in_words_to_now event.date
      %td= event.user.name

%br

= link_to 'New Event', new_event_path
