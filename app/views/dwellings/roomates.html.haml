%p
  %h3 Roomates:
  %table.table
    - for user in @dwelling.users
      %tr
        %td= link_to user.name, user_path(user)
%p
  %h3 Pending Invites:
  = render :partial => 'invites/table', :locals => {:@invites => @dwelling.invites}
  = link_to 'New Invite', new_dwelling_invite_path(@dwelling)
