= form_for @user, url: profile_settings_user_path(@user) do |f|
  - if @user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  Fields marked with a * are required
  .field
    = f.label 'email*'
    = f.text_field :email
  .field
    = f.label :cellphone
    = f.label 'Must be in the format: X-XXX-XXXX'
    = f.text_field :cellphone
  .field
    = f.label :first_name
    = f.text_field :first_name
  .field
    = f.label :last_name
    = f.text_field :last_name
  .actions
    = f.submit 'Save'

